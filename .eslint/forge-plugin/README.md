# Vendored Forge ESLint Plugin 

This directory contains a snapshot of the internal `forge-eslint` plugin.

It is copied here so that the template can be used inside a sandboxes.

## Do Not Edit Manually
All files under `dist/` will be overwritten.
